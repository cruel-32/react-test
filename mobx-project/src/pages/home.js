import React, { Component } from 'react';

class Counter extends Component {
    render() {
        return (
            <div>
                <h1>React와 mobX를 활용한 상태관리와 React Router V4, 그리고 SSR까지 테스트하는 페이지입니다.</h1>
            </div>
        );
    }
}

export default Counter;
